// Event system service.
// This service is responsible for:
// - Having a server that takes events
// - ???

Promise.resolve().then(async () => {

}).catch((err) => {
  console.log('Service failed')
  console.error((err && err.stack) ? err.stack : err)
})
